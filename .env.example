# Media-server base URL (browser-reachable). Baked into the client at build time.
# Required for production deploys (Forgejo var PUBLIC_MEDIA_SERVER_URL).
# When unset, the client falls back to the documented LAN default for local dev.
PUBLIC_MEDIA_SERVER_URL=http://192.168.5.111:8080
